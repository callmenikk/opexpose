import { SafeAreaView } from "react-native";
import Router from "./Router/Router";


const App = () => {
  return (
      <SafeAreaView>
        <Router />
      </SafeAreaView>
  );
}

export default App